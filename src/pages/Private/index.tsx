import React from "react";
import { Typography } from "../../components";

const PrivatePage: React.FC = () => {
  return (
    <div>
      <Typography>Private Page - Protected</Typography>
    </div>
  );
};

export default PrivatePage;
